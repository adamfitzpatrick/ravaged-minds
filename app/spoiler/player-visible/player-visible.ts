import * as angular from "angular";
import { PlayerVisibleController } from "./player-visible.component";

export const playerVisible: angular.IDirective = {
    controller: PlayerVisibleController
};
