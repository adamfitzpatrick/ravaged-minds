import * as angular from "angular";
import {PlayerClickableController} from "./player-clickable.component";

export const playerClickable: angular.IDirective = {
    controller: PlayerClickableController
};
