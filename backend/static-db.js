const staticData = require("./load-static-data");

module.exports = {
    stories: staticData.stories,
    maps: staticData.maps,
    entities: staticData.entities,
    synopses: staticData.synopses
};
